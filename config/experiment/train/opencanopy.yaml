# @package _global_
defaults:
  - override /model: pixelwise_regression

data:
  _target_: eosfm.datamodules.OpenCanopyDataModule
  batch_size: 32
  num_workers: 16
  root_dir: data

trainer:
  max_epochs: 20
  check_val_every_n_epoch: 1
  log_every_n_steps: 50

logger:
  name: opencanopy
